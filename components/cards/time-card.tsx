import { BaseCard } from "./base-card";

export const TimeCard = () => {
  return (
    <BaseCard
      type="TIME"
      topValue="+10"
      centerIcon="⏰"
      category="UTIL"
      mainColor="#FCA311" // Pomarańczowy (przykładowy)
    />
  );
};
