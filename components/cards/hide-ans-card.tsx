import { BaseCard } from "./base-card";

export const HideAnsCard = () => {
  return (
    <BaseCard
      type="HIDE"
      topValue="2"
      centerIcon="H"
      category="UTIL"
      mainColor="#00BBF9" // Niebieski (przykładowy)
    />
  );
};
